# Required for live Google Lens search through SerpApi.
SERPAPI_KEY=

# Optional: only required when --chain sepolia is used.
SEPOLIA_RPC_URL=
SEPOLIA_PRIVATE_KEY=

# Comma-separated browser origins allowed to call the local API.
FACECHAIN_ALLOWED_ORIGINS=http://localhost:3000
